<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangsiqian.tourist.example.ExampleDAO">
    <resultMap id="example" type="com.wangsiqian.tourist.example.representation.ExampleRepresentation">
        <result column="example_id" property="exampleId"/>
        <result column="example_field" property="exampleField"/>
    </resultMap>

    <select id="getExampleById" resultMap="example">
        SELECT
            example_id, example_field
        FROM
            example
        WHERE
            example_id=#{example_id}
    </select>

    <insert id="insertExample" parameterType="com.wangsiqian.tourist.example.model.Example">
        INSERT INTO example(example_id, example_field, created_at)
        VALUES (
            #{example.exampleId, jdbcType=VARCHAR},
            #{example.exampleField, jdbcType=VARCHAR},
            #{example.createdAt, jdbcType=TIMESTAMP}
        )
    </insert>
</mapper>
