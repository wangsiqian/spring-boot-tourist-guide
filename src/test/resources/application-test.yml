spring:
  datasource:
    # 测试数据库
    url: jdbc:mysql://mysql:3306/example_test?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password:
    schema: classpath*:mysql/example.sql
    continue-on-error: true
    initialization-mode: always
    sql-script-encoding: utf-8
